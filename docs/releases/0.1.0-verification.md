# v0.1.0 release-candidate verification

Verified locally on 2026-08-12 with Node.js 24.14.0 and pnpm 11.16.0. The project supports Node.js 22 or newer.

## Automated checks

| Check                            | Result                                                                                       |
| -------------------------------- | -------------------------------------------------------------------------------------------- |
| `pnpm install --frozen-lockfile` | Passed; lockfile and 21-workspace install are current                                        |
| `pnpm typecheck`                 | Passed                                                                                       |
| `pnpm lint`                      | Passed                                                                                       |
| `pnpm format:check`              | Passed                                                                                       |
| `pnpm build`                     | Passed for all 20 buildable workspace projects                                               |
| `pnpm test`                      | Passed; 44 of 44 tests                                                                       |
| `pnpm test:e2e`                  | Passed; mandatory end-to-end case                                                            |
| `pnpm test:security`             | Passed; 20 of 20 dedicated security tests                                                    |
| `pnpm demo`                      | Passed; five-agent chain completed with a score of 88.75/100                                 |
| `pnpm mawl doctor`               | Healthy; agents, prompts, workflows, sandbox, MCP configuration, and mock provider validated |
| `pnpm security:demo`             | Passed; hostile MCP output was quarantined and unauthorized authority was denied             |
| Local Markdown link check        | Passed across 24 Markdown files before this report was added                                 |

## Demo evidence

The default no-key demo completed this chain:

```text
orchestrator -> researcher -> analyst -> reviewer -> evaluator
```

It reported five completed tasks, four delegation edges, a maximum depth of four, 431 mock tokens, no tool calls, and a delegation score of 88.75/100. Run identifiers, timing, and trace paths vary on each execution.

The security demonstration confirmed:

- hostile MCP output remained untrusted and quarantined;
- a non-allowlisted shell tool request returned `PERMISSION_DENIED`;
- delegated `system.admin` authority was rejected;
- no secret was transferred; and
- an independent safe task still completed.

## Source hygiene

- Runtime traces, local databases, build output, dependencies, logs, coverage, temporary files, and sandbox output are excluded from the release archive.
- `.env.example` contains placeholders only; no live environment file is included.
- A high-confidence credential-pattern scan is part of final archive preparation.
- The public source contains no milestone reports or internal planning artifacts.

## Release decision

The source is ready for a v0.1.0 release candidate with the limitations documented in the [release notes](0.1.0.md), [capability matrix](../../REPOSITORY_CAPABILITIES.md), and [threat model](../threat-model.md). Repository creation, tagging, pushing, package publication, and GitHub release publication remain manual maintainer actions.
